# 脳波（eSense）データの解析

元フォルダ: `2024_10`

簡易脳波計のログ（`eSenseData.txt`）を JSON パースして DataFrame 化し、
attention / meditation と各周波数帯（delta〜highGamma）のパワーを可視化した発表用の解析。

- `(10.07)first round.py` → `(10.08)second_roundby night.py` → `(10.09)third round.py` の順に改訂
- `(10.09)third round.py` は Colab「発表用解析2 -ほぼ確定-」のエクスポート
- `(10.01)発表用解析2　_ほぼ確定_.ipynb` は中身が空（2バイト）
