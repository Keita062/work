# work リポジトリ運用ルール

このファイルはセッション開始時に自動で読み込まれる。日付フォルダに依存せず、
「今どのテーマに取り組んでいるか」だけで作業を継続できるようにするための
ルールをまとめたもの。

## ディレクトリ構成

```
work/
├── CLAUDE.md
├── .claude/
│   └── commands/
├── knowledge/          # 再利用可能なノウハウ（時系列を持たない）
│   ├── README.md
│   ├── slides/         # スライド作成の原則・テンプレート・過去の良い例
│   ├── analysis/       # データ分析・統計処理のお作法
│   ├── career/         # キャリア関連メモ
│   └── snippets/       # 再利用するコード片・プロンプト
├── projects/           # 進行中の作業（トピック名のみ。日付は使わない）
│   └── <topic-name>/
│       ├── STATUS.md
│       ├── notebooks/
│       ├── data/
│       └── output/
├── inbox/              # まだテーマ分けするほどでもない単発の作業
├── archive/            # 完了したプロジェクト（トピック名のまま移動）
├── templates/
│   └── STATUS.md       # 新規プロジェクト作成時にコピーして雛形
├── .venv
├── .gitignore
└── README.md
```

## 新しい作業を始めるときの判断

新しいタスクを頼まれても、日付でフォルダを作ることはしない。まず以下を判断する。

- `projects/` 配下の既存テーマの続き → 該当 `STATUS.md` を読んで再開する
- 明確に新しいテーマ → `projects/<topic-name>/` を新規作成する。
  `templates/STATUS.md` をコピーして使う
- テーマが定まらない単発作業 → `inbox/` に置く

フォルダ名は英数字とハイフンの kebab-case で、内容を表す名詞句にする
（例: `ssdse-timeseries`, `prompt-asset-security-review`）。

## タスク種別ごとのルーティング

作業を始める前に、タスクの種類に応じて先に読むべきファイルを確認する。

- スライド作成タスク → `knowledge/slides/principles.md` を先に読む
- データ分析タスク → `knowledge/analysis/` 配下を確認する
- 既存プロジェクトの継続 → 該当 `projects/<topic-name>/STATUS.md` を先に読む

## セッション終了の判定

返信の中で以下のいずれかに該当すると判断した場合、区切りとみなす。
応答の最後に「今回の作業はここまでで終了しますか？」と確認する。
毎回ではなく、区切りと思われるタイミングでのみ行う。

該当条件（いずれか）:
- 依頼されたタスクが完了し、次の指示を待っている状態
- ユーザーが「ありがとう」「OK」など区切りを示す発言をした
- 作業内容の指示がしばらく来ておらず、会話が落ち着いている

## 終了確認への応答

上記の確認に対して「終わった」「うん」「そうしといて」など
肯定的な返答があった場合、追加の指示を待たずに以下を自動実行する。

1. `STATUS.md` を更新する（現在の状態／次にすること／詰まっている点／
   今回わかったこと）
2. 「今回わかったこと」の中に他プロジェクトでも再利用できそうな知見が
   あれば、該当する `knowledge/` 配下のファイルへの転記を提案し、
   合意を得てから転記する
3. 「このプロジェクトは完了か」を確認し、完了していれば
   `projects/` から `archive/` に移動する
4. 変更内容が意味のあるコミットメッセージで `git commit` する
   （実行前に内容を確認する）

## ナレッジ蓄積の方針

- `knowledge/` はプロジェクトの都合に左右されない、汎用的な知見だけを置く
- 特定プロジェクト固有の詳細は `projects/<topic-name>/` に留め、
  `knowledge/` には抽象化・一般化した形で書く
- スライド関連の知見は特に、良かった構成・レビューで指摘された点を
  `knowledge/slides/principles.md` に蓄積していく
